# WP00 pilot evidence

Store sanitized WP00 task and gate reports here using `YYYY-MM-DD_<gate>_<environment>_<short-sha>.md`. Create a report only when its candidate commit SHA is known; keep sensitive proof in the approved restricted store.

# Test fixtures

Fixtures are synthetic, minimal, versioned when reused, and labeled where a user could mistake them for real data. Never commit secrets, student data, private source text, ordinary chat content, signed URLs, or real provider payloads.
